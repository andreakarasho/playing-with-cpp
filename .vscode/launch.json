{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "LLDB: C++ Launch (GDB-style)",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/zig-out/bin/cpptest2.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build C++" // Reference the build task from tasks.json
        }
    ]
}
